{
  "name": "pokedot",
  "private": true,
  "workspaces": {
    "packages": [
      "pokedot-client",
      "pokedot-server"
    ]
  },
  "dependencies": {
    "@commitlint/cli": "^12.1.1",
    "@commitlint/config-conventional": "^12.1.1"
  },
  "devDependencies": {
    "husky": "^6.0.0"
  },
  "scripts": {
    "postinstall": "husky install"
  }
}
